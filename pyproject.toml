[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "novaops-company-brain"
version = "0.1.0"
description = "Permission-sensitive HR/onboarding and IT operations company brain"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "boto3>=1.40,<2",
  "httpx>=0.27,<1",
  "langfuse>=4,<5",
  "mcp>=1.20,<2",
  "fastapi>=0.115,<1",
  "jsonschema>=4.20,<5",
  "pydantic>=2.10,<3",
  "python-dotenv>=1,<2",
  "PyYAML>=6,<7",
  "uvicorn>=0.34,<1",
]

[project.optional-dependencies]
dev = ["pytest>=8,<10"]
openai = ["openai>=2,<3"]
opensearch = ["opensearch-py>=2,<4"]

[tool.setuptools]
py-modules = ["dev_opensearch_server", "model_client", "bedrock_client", "retrieval_mcp_server"]

[tool.setuptools.packages.find]
include = ["company_brain*", "maya*", "renewal*", "service*", "vendor*", "webex*"]

[project.scripts]
novaops-vendor-worker = "vendor.worker:main"
novaops-renewal = "renewal.worker:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
